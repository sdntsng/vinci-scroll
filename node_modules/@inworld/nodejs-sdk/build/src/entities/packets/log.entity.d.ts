import { LogsEvent as ProtoLogsEvent } from '../../../../proto/ai/inworld/packets/packets_pb';
import { LogLevel, LogsEventLogDetail } from '../../common/data_structures';
export declare class LogsEvent {
    readonly text: string;
    readonly level: LogLevel;
    readonly metadata: Record<string, string>;
    readonly details: LogsEventLogDetail[] | undefined;
    constructor({ text, level, metadata, details, }: {
        text: string;
        level: LogLevel;
        metadata: Record<string, string>;
        details: LogsEventLogDetail[] | undefined;
    });
    static fromProto(proto: ProtoLogsEvent): LogsEvent;
    private static getLogLevel;
}
