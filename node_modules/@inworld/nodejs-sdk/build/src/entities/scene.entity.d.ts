import { CurrentSceneStatus } from '../../../proto/ai/inworld/packets/packets_pb';
import { Character } from './character.entity';
export interface SceneProps {
    name: string;
    characters?: Character[];
    description?: string;
    displayName?: string;
}
export declare class Scene {
    readonly name: string;
    readonly characters: Array<Character>;
    readonly description: string;
    readonly displayName: string;
    constructor(props: SceneProps);
    static serialize(scene: Scene): string;
    static deserialize(json: string): Scene;
    static fromProto(proto: CurrentSceneStatus): Scene;
    getCharactersByIds(ids: string[]): Character[];
    getCharactersByResourceNames(names: string[]): Character[];
}
